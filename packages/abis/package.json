{
  "name": "@amplestocks/abis",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "description": "Generated ABIs and typed contract configs for Amplestocks (wagmi CLI codegen from the Foundry artefacts)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./generated": "./src/generated.ts",
    "./package.json": "./package.json"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "generate": "node scripts/forge-build.mjs && wagmi generate && node scripts/postprocess.mjs",
    "build": "node -e \"console.log('[abis] src/generated.ts is committed; nothing to build')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "viem": "^2.56.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@wagmi/cli": "^2.10.0",
    "typescript": "^5.9.3",
    "vitest": "4.1.11"
  }
}
