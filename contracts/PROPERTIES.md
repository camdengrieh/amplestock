# Properties

> Generated by the Fizz skill. Each property is described in English and has one
> of three states:
> - `[ ]` pending — not yet implemented; `/fizz-convert` will pick it up
> - `[x]` implemented — Solidity exists in Properties.sol / handlers
> - `[-]` skipped / manual — `/fizz-convert` will NOT touch it. Use this for TODO stubs,
>   properties that need human judgment, or properties you add yourself later.
>
> Users may freely add their own entries (use any unused `GL-NN` / `SP-NN` ID) and mark
> them `[ ]` to have `/fizz-convert` implement them, or `[-]` to leave them as a spec
> note only.
>
> IDs are stable. Do not renumber.
>
> **Guarantee tag.** Every property is tagged `SHOULD-HOLD` or `EXPLORATORY`:
> - `SHOULD-HOLD` — explicitly guaranteed by docs/spec/standard or an exact identity
>   (evidence cited inline). If the campaign violates it, that is a **confirmed bug**.
> - `EXPLORATORY` — inferred, not explicitly promised. A violation is a **lead flagged
>   for human review**, not an automatic bug.
>
> This tag is set at generation time so violations can be triaged without post-hoc
> severity guessing. If you edit a property's logic such that it is no longer guaranteed,
> change its tag to `EXPLORATORY`.

## Global Properties
> Always-on invariants. Implemented as `public function property_*()` in `test/fizz/Properties.sol`.

- [x] **GL-01** — The AMPS held by every account the campaign can reach (the three actors, the vault, bond shell, hook, both routers, the PoolManager, genesis holder, team vesting, creator, team, standby and the fixture) sums to exactly `amps.totalSupply()`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — closed-form ERC-20 identity: OZ `ERC20._update` is the only writer of `_balances`/`_totalSupply` and moves them equal-and-opposite; `Amps` adds no hook, fee or rebase (`src/token/Amps.sol` NatSpec); plan I3; guard G-40; Priority: HIGH; Sources: CON-01, SPEC-01)
- [x] **GL-02** — Total supply never exceeds the genesis allocation plus everything the bond markets have ever issued: `totalSupply() <= S0 + Σ_m market(m).totalIssued`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I3/I10 rev-6 ("genesis + bonds up; redemption and the fee/buyback burn down; ask inventory never minted"); x-ray E-1 and X-6 (on-chain: Yes); guards G-7, G-40; Priority: HIGH; Sources: CON-02)
- [x] **GL-03** — The supply ledger closes: `amps.totalSupply() == S0 + ghosts.mintedByBonds − ghosts.burnedTotal` at every observation. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — x-ray E-1 (on-chain: Yes) enumerates every mint site (`VaultNavLib.genesisAllocate`, `AmpsVault.mintVesting`) and every burn site (`AmpsVault.sol:848/882`, `VaultPlacementLib.sol:257/383/1271`), all behind `Amps`' `onlyVault` (G-40); Priority: HIGH; Sources: VT-01, SPEC-04)
- [x] **GL-04** — The share token's roles are intact: `amps.balanceOf(address(0)) == 0` and `amps.vault() == address(vault)` for as long as no migration has run (the whole campaign). (Category: VALID_STATE; Guarantee: SHOULD-HOLD — G-40 (`Amps.sol:34`, "the whole of the share token's trust model: `totalSupply` moves only where the vault moves it"); the zero-address clause is OZ `_update`'s own `require`; Priority: MEDIUM; Sources: SPEC-04)
- [x] **GL-05** — `AmpsBonds` always holds at least the AMPS it still owes: `balanceOf(bonds) >= Σ_positions (principal − claimed)`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — x-ray I-6 (on-chain: Yes): the Δ-pairs `_positions[to].push({principal: issued})` ↔ `mintVesting(address(this), ampsOut)` (`AmpsBonds.sol:411-419 ↔ :454`) and `record.claimed = vested` ↔ a transfer of `vested − claimed` (`:566 ↔ :569`); inequality because donation is possible; Priority: HIGH; Sources: CON-06, ST-09, SPEC-23)
- [x] **GL-06** — Per-market issuance ledger: the principals of every vesting position written against market `m` sum to exactly `market(m).totalIssued`, and Σ claimed <= Σ principal <= Σ totalIssued. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — closed-form Δ-pair in `bond` step 6: `stored.totalIssued += issued` and `_positions[to].push({principal: issued})` written from the same local in the same block, neither field having another write site (`AmpsBonds.sol:404-419`); plan I28; Priority: MEDIUM; Sources: CON-07, SPEC-25)
- [x] **GL-07** — For every market, `issuedThisEpoch <= totalIssued`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — both fields are incremented by the same `issued` in `bond` step 6 and `issuedThisEpoch` is only ever reset to zero by `_rollEpoch`, so containment is exact; Priority: MEDIUM; Sources: CON-08)
- [x] **GL-08** — Splitting one large bond into many small ones does not bypass the caps: cumulative per-market epoch issuance and cumulative global daily issuance stay within the cap measured against the supply at the window's start, plus the documented ~1% self-inflation slack. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; Sources: ADV-11, CON-08)
- [x] **GL-09** — The vesting book is internally consistent for every position: `claimed <= vested(t) <= principal`, `claimableTotal(owner) + unvestedOf(owner) == Σ (principal − claimed)`, and a fully elapsed position is exactly fully vested. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `Types.sol:325` "`claimed <= principal` always"; `claimableTotal` sums `vested − claimed` and `unvestedOf` sums `principal − vested`, so the two partition `principal − claimed` exactly (`AmpsBonds.sol:256-272`); plan I28; Priority: HIGH; Sources: CON-29, VS-05, SPEC-21)
- [x] **GL-10** — A vesting position is immutable except for `claimed`: `principal`, `start`, `vestSeconds` and `marketId` never change after creation, and `claimed` is monotone non-decreasing and equals the cumulative amount the campaign has seen claimed. (Category: VARIABLE_TRANSITION — tracks `VestingPosition.claimed`; Guarantee: SHOULD-HOLD — `claimed` is written only as `record.claimed = uint128(vested)` with `vested = _vested(record) <= principal` (`AmpsBonds.sol:566`, `:585`, `_vested` at `:1206-1213`); Priority: HIGH; Sources: RT-05, VT-03, SPEC-22)
- [x] **GL-11** — `claimable(owner, id)` is monotone non-decreasing in time for a position that has not been claimed from, never exceeds the undelivered principal, and is untouched by `setVestSeconds`, a market pause, a policy swap, a retired constituent or a guardian freeze. (Category: VARIABLE_TRANSITION — tracks `claimable(owner,id)`; Guarantee: SHOULD-HOLD — plan I38 ("`claim()` … succeeds regardless of collateral removal, market pause, policy swap or guardian freeze") and plan I28 ("`claimable(t)` monotone in `t`"); `AmpsBonds.sol:1203-1205` "the position's own `vestSeconds` is used, never the governed one"; Priority: MEDIUM; Sources: RT-06, SPEC-22)
- [x] **GL-12** — `bonds.positionCount(owner)` is monotone non-decreasing for every owner. (Category: VARIABLE_TRANSITION — tracks `positionCount`; Guarantee: SHOULD-HOLD — `_positions[to]` is only ever `push`ed (`AmpsBonds.sol:411`); no `pop`, `delete` or length write exists in the contract; Priority: MEDIUM; Sources: VT-04)
- [x] **GL-13** — `market(id).totalIssued` is monotone non-decreasing for every market. (Category: VARIABLE_TRANSITION — tracks `BondMarket.totalIssued`; Guarantee: SHOULD-HOLD — `stored.totalIssued += issued` (`AmpsBonds.sol:407`) is the field's only write site; `Types.sol:316` documents it as issuance since inception; Priority: MEDIUM; Sources: VT-02)
- [x] **GL-14** — Every vesting position with `claimable > 0` can be claimed: no market closure, collateral removal, policy swap, guardian freeze or gate state makes a `claim` on it revert. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I38; `AmpsBonds.claim` NatSpec "Structurally ungated. The only reads are `_positions[msg.sender]` and the immutable {amps}"; x-ray I-6 "If violated — a vested position could not be claimed"; Priority: HIGH; Sources: ADV-02)
- [x] **GL-15** — A third party cannot grief a bonder by growing their position array: however many positions an attacker pushes onto a victim with `bond(..., to = victim)`, per-id `claim` still succeeds and its gas stays bounded. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — fix-log wave-1 lead "Third-party growth of a bonder's position array | Accepted (griefing of `claimAll`/lens only; **per-id `claim` unaffected**)"; plan I38; Priority: MEDIUM; Sources: ADV-27)
- [x] **GL-16** — A market that is closed or detached (`!open` or `marketIdOf(collateral) != marketId`) has not issued: its `totalIssued`, `issuedThisEpoch` and `lastBondAt` are unchanged across the call. (Category: VALID_STATE — when `market.open == false` or the collateral is detached; Guarantee: SHOULD-HOLD — guard G-34, the first thing `bond` does after its zero checks: `if (!stored.open || marketIdOf[stored.collateral] != marketId) revert MarketClosed(marketId)` (`AmpsBonds.sol:371`), ahead of every write at `:405-408`; Priority: HIGH; Sources: VS-10)
- [x] **GL-17** — The registry's recorded `marketId` and the bond shell's own attribution agree: `constituent(id).marketId != 0` implies `bonds.marketIdOf(token) == that marketId`, and any live market's `collateral` is that constituent's token. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — G-34's conjunction on the bond path ("a removed collateral leaves its record readable, so `open` alone would keep a detached market quoting") plus `_setMarketOpen`'s repair leg `if (live != stored) config.marketId = live;` (`PoolRegistry.sol:906`); x-ray X-9; Priority: MEDIUM; Sources: VS-04)
- [x] **GL-18** — NAV/share is exactly `floor((totalAssetsUsd18() + 1) × 1e18 / (totalSupply() + VIRTUAL_SHARES))`, the denominator is `Amps.totalSupply()` and nothing else, and reconstructing `A` from it (`mulDiv(nav, T + VIRTUAL_SHARES, WAD)`, what `spokeWeightBps` does) never overstates `A`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — x-ray I-8 (on-chain: Yes) `AmpsVault.sol:1497-1501`; `AmpsVault.spokeWeightBps` NatSpec "the inverse of {_navPerShare} up to the `+ 1` wei" (`:526-528`); plan I6, I22; Priority: HIGH; Sources: CON-14, RD-13)
- [x] **GL-19** — `A` decomposes exactly: `totalAssetsUsd18()` equals the sum over registered assets of `P_j × (ERC-6909 claim + idle ERC-20 + the pool position's counter side at the reference sqrt price)`, with nothing else in it and nothing counted twice. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `VaultNavLib.totalAssetsUsd18`'s own stated identity (`VaultNavLib.sol:105`); plan I5, I6, I7, I21; `LadderPositionValuer.amountsOf` "resolves the reference exactly as `VaultNavLib` does"; Priority: HIGH; Sources: CON-15)
- [x] **GL-20** — The asset registry is well formed: `assetCount()` is monotone non-decreasing, the list carries no duplicate, never carries AMPS, `isAsset` agrees with it, and AMPS never appears in a redemption basket. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I5 ("NAV values every AMPS leg at zero; `assets[AMPS].enabled == false`"); `_registerAsset` "AMPS is never an asset (I5), and re-registration is a no-op so the list can never carry a duplicate" (`AmpsVault.sol:1679-1685`); guard G-5; Priority: MEDIUM; Sources: CON-16, VT-06)
- [x] **GL-21** — `inventoryAmps()` equals the vault's idle AMPS plus its AMPS ERC-6909 claim plus the AMPS side of every ladder at the reference price, and excludes the AMPS `AmpsBonds` holds for vesting. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `VaultNavLib.inventoryAmps` NatSpec ("the idle balance, the ERC-6909 claim and the AMPS inside the vault's positions … it excludes the AMPS `AmpsBonds` holds for vesting (I30)"); plan I10, I30; Priority: MEDIUM; Sources: CON-17)
- [x] **GL-22** — The protocol is solvent against its own redemption promise: for every registered asset, what `previewRedeem(totalSupply())` would pay never exceeds what the vault holds of it (claims + idle) plus what a full ladder unwind would free. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I23; with `shares = T` the payout identity collapses to `b·(1 − fee/BPS) + released <= b + released`, a closed-form bound (`VaultRedeemLib._payout:667`); Priority: HIGH; Sources: SPEC-06)
- [x] **GL-23** — The PoolManager is solvent for the claims the protocol holds: for AMPS and every registered asset, its ERC-20 balance is at least the sum of outstanding ERC-6909 claims across every account the campaign can give claims to. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: MEDIUM; Sources: CON-26)
- [x] **GL-24** — Zero-state safety: `totalSupply() + VIRTUAL_SHARES > 0` and NAV/share is finite in every reachable state; when supply is zero `previewNavPerShareX18()` is exactly zero; and near-zero supply never lets a bond issue against a zero NAV/share or a buy take AMPS for nothing. (Category: VALID_STATE — when `totalSupply()` is zero or near zero; Guarantee: SHOULD-HOLD — plan I22 ("`T + VIRTUAL_SHARES > 0`; NAV/share finite for all reachable states"); x-ray I-8 "`supply == 0` short-circuits to zero rather than to `1e18 / VIRTUAL_SHARES`" (`AmpsVault.sol:1499`); Priority: HIGH; Sources: ADV-19, VS-06, SPEC-11)
- [x] **GL-25** — [MANDATORY, V-06] The first-depositor / donation inflation grief does not exist: after any sequence of bonds, redemptions, swaps and direct ERC-20 donations to the vault, hook or bond shell, a quote for one whole unit of collateral (`1e18`) on any open, quotable market with capacity and no named refusal still returns a non-zero `ampsOut`. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; Sources: SPEC-05)
- [x] **GL-26** — The disclosure surface never reverts in any reachable state — including after a hostile donation, a frozen gate, a paused issuer beacon and a stale feed: `checkpointData()`, `navPerShareX18()`, `previewNavPerShareX18()`, `inventoryAmps()`, `previewRedeem(1)`, `pot.quote(...)`, `bonds.dailyIssuance()` and `valuer.amountsOf(pool)` all answer. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `LadderPositionValuer` NatSpec "**Never gated, never reverting** (ruling 7) … all return `(0, 0)`"; fix-log wave-1 findings 2 and 3 (a constituent whose `balanceOf` reverts must not revert `previewRedeem`), wave-2 finding 2; Priority: HIGH; Sources: SPEC-11)
- [x] **GL-27** — The ungated redemption floor is always open: whenever the current actor holds shares, redeeming one wei of them succeeds and burns supply — never refused for a gate state, guardian freeze, stale feed, paused constituent, donation or placement cooldown. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I14 ("`redeemProRata` exempt by construction"); `AmpsVault.redeemProRata` NatSpec "The body below contains no reference to `_oracleGate`, `_registry`, `_GUARDIAN`, `_standbyVault`, a freeze timestamp, a pause flag or any price" (`:826-834`), proved at the storage level by `test/unit/GuardSymmetry.t.sol`; Priority: HIGH; Sources: SPEC-12, ADV-01, ADV-28)
- [x] **GL-28** — Splitting a redemption never pays more than doing it once: for any `a`, `b` and every asset, `previewRedeem(a)[i] + previewRedeem(b)[i] <= previewRedeem(a+b)[i]`, and the same for `inventoryBurned`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — closed-form: every payout term is a round-down `FullMath.mulDiv` and `floor(x·a/T) + floor(x·b/T) <= floor(x·(a+b)/T)`; the formula is x-ray I-9 (`VaultRedeemLib._payout:663-671`, `previewUnwind:849-864`, `redemption:631`); Priority: HIGH; Sources: RT-16)
- [x] **GL-29** — `previewRedeem` is monotone in shares: more shares never pays less of any asset and never burns less inventory. (Category: VARIABLE_TRANSITION — tracks `previewRedeem` amounts against `shares`; Guarantee: SHOULD-HOLD — every term of `VaultRedeemLib._payout` / `previewUnwind` is a `FullMath.mulDiv` floor of a quantity linear and non-decreasing in `shares`, and `SqrtPriceMath.getAmount{0,1}Delta` is non-decreasing in liquidity; stated as x-ray I-9; Priority: MEDIUM; Sources: RT-17)
- [x] **GL-30** — There is no free round trip through a protocol-owned ladder on the quoter: buying AMPS with `x` of a pool's counter and immediately selling the AMPS back into the same pool quotes strictly less than `x`. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; Sources: SPEC-17, RT-01)
- [-] **GL-31** — No actor gains USD value it was not funded with: for every actor, `assetsUsd18Of(a) + amps.balanceOf(a)·navPerShare` never exceeds its recorded basis plus everything the harness minted to it — covering the redemption round trip and the placement sandwich alike. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; Sources: ADV-08, ADV-30)
- [x] **GL-32** — Keeper-callable upkeep does not bleed the vault by a thousand cuts: the cumulative NAV/share lost across every placement, compound, rollout and bonded deployment in the campaign stays under 100 bp of the NAV/share high-water mark. (Category: VARIABLE_TRANSITION — tracks cumulative drift of `navPerShareX18`; Guarantee: EXPLORATORY; Priority: HIGH; Sources: ADV-12)
- [x] **GL-33** — The reference price never rounds below NAV and never outruns its rate cap: `pRefX18() >= navPerShareX18()` always, a downward move lands on `P_mkt` (or NAV, whichever is higher), and an upward move never exceeds `pRefPrev·refUpRateBps·elapsed/(3600·BPS)`. (Category: VARIABLE_TRANSITION — tracks `pRefX18`; Guarantee: SHOULD-HOLD — x-ray I-11 ("`P_ref = max(navPerShareX18, rateLimited(P_mkt))` … The NAV floor is unconditional at `VaultNavLib.sol:382`"); plan I24; Priority: MEDIUM; Sources: RD-18)
- [x] **GL-34** — Every governed scalar sits inside its hard band at all times: `redeemFeeBps ∈ [0, REDEEM_FEE_BPS_MAX]`, `ampsFeeBps ∈ [AMPS_FEE_BPS_MIN, AMPS_FEE_BPS_MAX]`, each pool's `buyFeeBps` inside its class band, and every bond parameter inside `Constants`. (Category: VARIABLE_TRANSITION — tracks the governed scalars; Guarantee: SHOULD-HOLD — G-24 (`AmpsVault.sol:1702`, "the single band site every governed vault scalar funnels through"), x-ray I-1 (`redeemFeeBps ∈ [0, 500]`), I-2 (`ampsFeeBps ∈ [100, 600]`); Priority: MEDIUM; Sources: SPEC-32)
- [x] **GL-35** — `creatorBpsAt(t)` is monotone non-increasing in `t`, equals `CREATOR_FEE_BPS` at genesis, never exceeds it, and is exactly zero from `genesisTimestamp + CREATOR_DECAY_SECONDS` onward. (Category: VARIABLE_TRANSITION — tracks `creatorBpsAt`; Guarantee: SHOULD-HOLD — x-ray I-21 gives the closed form `CREATOR_FEE_BPS × max(0, 1 − (t − genesisTimestamp)/CREATOR_DECAY_SECONDS)` (`AmpsVault.sol:574-583`, duplicated at `VaultPlacementLib.sol:1566-1572`); plan I31; Priority: MEDIUM; Sources: RD-14, VT-12, ADV-13)
- [x] **GL-36** — Cumulative value paid to `vault.creator()` in every currency stays at or below cumulative swap volume × `CREATOR_FEE_BPS`, and nothing at all is paid after the decay window closes. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I31 as amended by revision 6/7 ("creator ≤ volume × creatorBps in every currency"; "creator payouts are the only transfer of protocol-held AMPS to a non-pool address"); x-ray I-21; Priority: MEDIUM; Sources: ADV-13, CON-27)
- [x] **GL-37** — The rotation credit is transaction-scoped: no account carries a non-zero `rotationCredit` between handler calls. (Category: VARIABLE_TRANSITION — tracks `AmpsHook` transient `ROTATION_CREDIT`; Guarantee: SHOULD-HOLD — plan I26 ("`tload(ROTATION_CREDIT)` … **zero at the start of every transaction**"), with the rev-6 amendment limiting the credit to the router's ROTATE hops; the counters live in EIP-1153 transient storage; Priority: MEDIUM; Sources: CON-28)
- [x] **GL-38** — The hook is custody-free and delta-free: it holds no ERC-6909 claim of AMPS or any registered asset, never pays a token out (a donation is trapped, never moved), and both swap callbacks return a zero delta. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I13 ("Hook holds zero ERC-20/ERC-6909; no returns-delta bit; `beforeSwap` returns `ZERO_DELTA`, `afterSwap` returns 0; no `donate()` or `poolManager.swap()` anywhere in the codebase"); Priority: HIGH; Sources: CON-12, ADV-18)
- [x] **GL-39** — `AmpsRouter` holds nothing between calls: zero AMPS, zero of every registered counter asset, zero ERC-6909 claims and zero ether. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `AmpsRouter` NatSpec "the assets it touched are swept to `msg.sender` on a best-effort basis"; `_sweep` "The invariant that matters is that this contract holds nothing *between* transactions" (`AmpsRouter.sol:426-435`); Priority: MEDIUM; Sources: CON-20, ADV-17)
- [x] **GL-40** — The live-cell counter is honest and bounded: `vault.liveCells()` equals the number of placement records across all pools that actually hold liquidity, and that count never exceeds `Constants.MAX_LIVE_CELLS`. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; Sources: CON-09, VS-01, SPEC-28, ADV-20)
- [x] **GL-41** — Internal accounting equals external reality: the liquidity the vault records for a pool equals the liquidity the PoolManager actually holds for the vault's positions on that pool's canonical grid. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; Sources: CON-10)
- [x] **GL-42** — Grid discipline: every live `PlacementRecord` is exactly one canonical cell of its pool's doubling grid (`upper − lower == doublingTicks`, on-grid, cell index inside `[GRID_MIN_M, GRID_MAX_M)`), and no two live records in one pool share a cell or a `lowerTick`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — G-29 (`VaultPlacementLib.sol:855`, `OffGrid`: "Every bucket must be exactly one cell of the pool's canonical doubling grid, which is what the position valuer enumerates"); `_writeRecords` merges by lower tick rather than pushing (`:903-916`); plan I34; Priority: HIGH; Sources: ST-14, SPEC-27)
- [x] **GL-43** — Ladder sidedness holds for every live cell: an ask lies strictly above the tick it was placed against and holds only AMPS; a bid lies strictly below it and holds only the counter asset. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I9 ("Asks (AMPS-only) lie strictly above the tick, bids … strictly below, unconditionally"); G-30 (`VaultPlacementLib.sol:873`, "Sidedness in v4's own terms … so no cell is straddled at placement"); Priority: HIGH; Sources: ADV-34)
- [x] **GL-44** — `ladderLength(pool)` is monotone non-decreasing and never exceeds `Constants.GRID_CELLS`. (Category: VARIABLE_TRANSITION — tracks `ladderLength`; Guarantee: SHOULD-HOLD — `if (n >= Constants.GRID_CELLS) revert OffGrid(...)` immediately before the only `records.push` (`VaultPlacementLib.sol:914-916`); no `pop`/`delete` exists in `src/vault/**`; Priority: MEDIUM; Sources: VT-07)
- [x] **GL-45** — `vault.lastPlacementAt(pool)` is monotone non-decreasing and never ahead of `block.timestamp`. (Category: VARIABLE_TRANSITION — tracks `lastPlacementAt`; Guarantee: SHOULD-HOLD — the only writes are `cooldown[poolId] = uint32(block.timestamp)` (`VaultPlacementLib.sol:288`, `:466`; `VaultRolloutLib.sol:190-192`, `:322`); Priority: LOW; Sources: VT-08)
- [x] **GL-46** — The checkpoint's `timestamp` and `blockNumber` are monotone non-decreasing and never ahead of the chain. (Category: VARIABLE_TRANSITION — tracks `Checkpoint.timestamp`/`.blockNumber`; Guarantee: SHOULD-HOLD — `_checkpointTimestamp = uint32(block.timestamp); _checkpointBlock = uint32(block.number);` (`AmpsVault.sol:1533-1534`) are the fields' only write sites, and `_checkpoint`'s `elapsed` underflows on a future stamp; Priority: MEDIUM; Sources: VT-09)
- [x] **GL-47** — A pool's `highWaterTick` only rises, except on a vault-driven `resetHighWater`. (Category: VARIABLE_TRANSITION — tracks `highWaterTick`; Guarantee: SHOULD-HOLD — `if (truncatedTick > s.highWaterTick) s.highWaterTick = truncatedTick;` (`TruncatedOracleLib.sol:309`) is a rise-only write, and `resetHighWater` writes `min(lastTruncatedTick, floorTick)` (`:332-335`) behind `onlyVault` (`AmpsHook.sol:1577`); plan I33; the generated "never sits above `lastTruncatedTick`" clause was removed after the 2026-09-11 campaign — the mark is a running maximum, so it sits above the tick in force after any up-then-down block and below it after a reset; Priority: HIGH; Sources: VT-10)
- [x] **GL-48** — A pool's `observationCoverage` is monotone non-decreasing while the clock only moves forward. (Category: VARIABLE_TRANSITION — tracks `observationCoverage`; Guarantee: EXPLORATORY; Priority: LOW; Sources: VT-11)
- [x] **GL-49** — Rollout cannot drain the entry pools: cumulative movement over any rolling day stays within the decayed allowance, the entry pools are never taken below `entryFloorBps` of movable ask inventory, and a refused rollout burns no allowance. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I32 ("Rollout moves ≤ `rolloutBpsPerDay` of the POL tranche per day … never takes the entry pools below `entryFloorBps`"); x-ray G-32, G-33, I-19, E-4; fix-log wave-4 finding 12 ("`moved` decays linearly … `windowStart` advances on **every** charge"); Priority: MEDIUM; Sources: ADV-32)
- [x] **GL-50** — `activeConstituentCount()` equals the number of ids whose stored status is ACTIVE, and every id in `[1, constituentCount()]` is ACTIVE or RETIRED (never NONE, never FROZEN in this campaign). (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — the counter is written only beside a status write (`PoolRegistry.sol:290-291`, `:344-347`, `:370-374`), and the FROZEN overlay's two inputs have no reachable writer here; Priority: HIGH; Sources: VS-02, SPEC-31)
- [x] **GL-51** — A constituent is RETIRED if and only if it carries a retirement stamp: `status == RETIRED ⟺ retiredAt != 0`. (Category: VALID_STATE — when `status == RETIRED`; Guarantee: SHOULD-HOLD — the two writes are paired with the status in the same block: `config.status = RETIRED; … config.retiredAt = uint32(block.timestamp);` (`PoolRegistry.sol:344-346`) and `config.status = ACTIVE; … config.retiredAt = 0;` (`:370-377`); Priority: MEDIUM; Sources: VS-03)
- [x] **GL-52** — A retired name is an exit-only book: no RETIRED constituent has an open bond market, and none carries a non-zero rollout weight. (Category: VALID_STATE — when `status == RETIRED`; Guarantee: SHOULD-HOLD — plan I37 verbatim ("a retired constituent has no open bond market and zero rollout weight"), enforced at `PoolRegistry.sol:353` and `:414-416`; x-ray X-9; fix-log wave-1 finding 10 (`deployBonded` returns 0 for a non-ACTIVE name); Priority: HIGH; Sources: ST-04, SPEC-30)
- [x] **GL-53** — The index weight vector normalises: the target weights of every ACTIVE constituent sum to exactly `BPS` and each sits inside `[indexFloorBps(), indexCapBps()]`; and no market's observed discount exceeds its `dMaxBps`. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: MEDIUM; Sources: CON-30, ADV-33, SPEC-31)
- [x] **GL-54** — The population counters are monotone non-decreasing and bounded: `constituentCount() <= MAX_CONSTITUENTS`, `marketCount <= MAX_COLLATERALS`, and `poolCount()` never falls. (Category: VARIABLE_TRANSITION — tracks the three population counters; Guarantee: SHOULD-HOLD — `if (marketCount >= Constants.MAX_COLLATERALS) revert CollateralSetFull(...)` then `marketId = marketCount + 1` (`AmpsBonds.sol:615`, `:635-636`); `Constants.sol:721-722`; plan I37 ("`n ≤ MAX_CONSTITUENTS`"); Priority: LOW; Sources: VT-05)
- [x] **GL-55** — A held-back feed jump implies there is a latch to hold it against, and a different round: `pendingAnswer(t).roundId != 0` implies `acceptedAnswer(t).answerUsd8 != 0` and the two round ids differ. (Category: VALID_STATE — when a pending answer is held back; Guarantee: SHOULD-HOLD — a `Pending` is written only on the latch branch (`FeedRegistry.sol:667-678`) and only when `againstLatch && pending.roundId != roundId` (`:372-378`); `_latch` deletes it (`:749`); Priority: MEDIUM; Sources: VS-07)
- [x] **GL-56** — A live protocol freeze outranks every other layer: while `protocolFreezeUntil() > block.timestamp`, `gate.state(id)` and `gate.stateByPool(p)` report `SCHEDULED_FREEZE` for every constituent and pool. (Category: VALID_STATE — when a protocol or constituent freeze is live; Guarantee: SHOULD-HOLD — `_resolveState` is documented as "The precedence order, most restrictive first" and tests `frozen` first: `if (frozen) return GateState.SCHEDULED_FREEZE;` (`OracleGate.sol:958`, with `_protocolFrozen()` at `:981`); Priority: MEDIUM; Sources: VS-08)
- [x] **GL-57** — Every guardian freeze is bounded and expires: a set freeze stamp is strictly in the future and at most `GUARDIAN_FREEZE_MAX_SECONDS` ahead, and an unfreeze leaves it exactly zero. (Category: VALID_STATE — when a freeze stamp is set; Guarantee: SHOULD-HOLD — guard G-43 / x-ray I-22: `_requireFreezeWindow` reverts `OutOfBand("freezeUntil", …)` outside `[now+1, now + GUARDIAN_FREEZE_MAX_SECONDS]` (`OracleGate.sol:991-995`); `unfreezeProtocol` writes 0 (`:648`); Priority: HIGH; Sources: ST-19, ADV-28)
- [x] **GL-58** — The gate's layer-A watchdog stamp only advances: `(lastBlock, lastTimestamp)` are monotone non-decreasing and never ahead of the chain. (Category: VARIABLE_TRANSITION — tracks the watchdog stamp; Guarantee: SHOULD-HOLD — `_stamp()` writes `_lastBlock`/`_lastTimestamp` (`OracleGate.sol:804-805`) and nothing else writes either field; Priority: LOW; Sources: VT-13)
- [x] **GL-59** — The launch latches never come back down: `genesisMinted()` and `initialized()` are true for the whole campaign and `genesisTimestamp()` never changes. (Category: VALID_STATE — when the vault is initialized; Guarantee: SHOULD-HOLD — x-ray I-14 and I-15 (both on-chain: Yes): "`_genesisMinted: false -> true`, one-way … there is no reverse path" (`AmpsVault.sol:999`), "`_initialized: false -> true`, one-way" (`:1035-1036`); Priority: MEDIUM; Sources: VS-09)
- [x] **GL-60** — The keeper purse is segregated and self-limiting: cumulative payouts never exceed cumulative funding, the pot never pays more than it holds, `spentLast24h()` never exceeds `dailyCeilingUsd18()`, no rolling 24 hours pays more than two ceilings, and the pot is never a valued asset. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I21 ("`BountyPot` excluded from `A`; never pays more than it holds; depleted ⇒ unpaid, not reverting"); x-ray I-20 ("no 24-hour interval can pay out more than two ceilings") and E-5; Priority: HIGH; Sources: VT-14, ADV-14, SPEC-33)
- [x] **GL-61** — No transient lock is ever left armed: after any handler call a fresh permissionless entry point (`vault.checkpoint()`, `ampsRouter.buy`) never reverts with `Reentrancy`. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; Sources: ADV-06)
- [x] **GL-62** — Narrow storage types never brick the protocol: `navPerShareX18`, `pRefX18`, every market's `totalIssued` and the pot's window all stay strictly inside their `uint128` bounds, and `checkpoint()` never panics on a narrow cast however far the feeds and display multipliers are walked. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; Sources: ADV-23)
- [x] **GL-63** — No privilege escalation: every `onlyTimelock` setter, `onlyVault` entry point, `onlyGuardian` freeze, registry-only and bonds-only call, and both `unlockCallback`s revert for any other caller. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — x-ray guards G-3 (`AmpsVault.sol:372`), G-4 (`:939`), G-10 (`:1057`), G-16 (`:1320`), G-19 (`:1402`), G-40 (`Amps.sol:34`); X-6 and X-8; Priority: HIGH; Sources: ADV-25)
- [x] **GL-64** — `PriceLib`'s USD-value / raw-amount pair round trips in the protocol's favour: valuing the amount required to cover a USD value never reports less than that value, and re-deriving the required amount from a valued balance never asks for more than the balance. (Category: VARIABLE_TRANSITION — tracks `counterValueUsd18` / `counterAmountFromUsd18`; Guarantee: SHOULD-HOLD — closed-form: `counterAmountFromUsd18(v) = ceil(v·S/P)`, `counterValueUsd18(r) = floor(r·P/S)`, so `floor(ceil(v·S/P)·P/S) >= v` and `ceil(floor(r·P/S)·S/P) <= r`; plan I20 and the two functions' own rounding NatSpec; Priority: HIGH; Sources: RT-10)
- [x] **GL-65** — The USD-price / v4-price conversion round trips within one tick and never recovers a price above the one that produced it, both in `PriceLib` and across the `GatePriceMath` boundary the gate actually uses. (Category: VARIABLE_TRANSITION — tracks `ampsPerCounterToSqrtPriceX96` / `sqrtPriceX96ToAmpsPriceUsd18`; Guarantee: SHOULD-HOLD — plan I20 ("`PriceLib` round trips within 1 tick; rounding always favours the protocol"); the forward leg "Rounds **up** … so a ladder anchored here never sells AMPS below its reference price", the reverse "Rounds **down**"; Priority: HIGH; Sources: RT-11)
- [x] **GL-66** — `tick -> sqrtPrice -> tick` is the identity, and `alignTick` is idempotent and directional (`alignTick(t,s,false) <= alignTick(t,s,true)`, both multiples of the spacing). (Category: VARIABLE_TRANSITION — tracks `alignTick` / `sqrtPriceX96ToTick`; Guarantee: SHOULD-HOLD — `tickToSqrtPriceX96` "Exact for every tick in range"; `sqrtPriceX96ToTick` "Rounds **down** by construction: returns the greatest tick whose sqrt price is `<= sqrtPriceX96`"; `alignTick` NatSpec on the floor/ceil pair and the usable-range clamp; Priority: MEDIUM; Sources: RT-12)
- [x] **GL-67** — `LadderLib`'s amount / liquidity pair never gives back more than it was given, on both the AMPS and the counter side. (Category: VARIABLE_TRANSITION — tracks `liquidityForAmount*` / `amount*ForLiquidity`; Guarantee: SHOULD-HOLD — `LadderLib` header "Every amount/liquidity conversion rounds **down**, so a placed position never claims more inventory than the vault holds and a valuation never overstates what a position contains"; Priority: HIGH; Sources: RT-13)
- [x] **GL-68** — `LadderLib`'s weight/split pair loses nothing: `sum(weights(tilt, n)) == 1e18` exactly, the weights are monotone non-decreasing in `k`, and `sum(split(amount, w)) == amount` exactly. (Category: VARIABLE_TRANSITION — tracks `weights` / `split`; Guarantee: SHOULD-HOLD — `LadderLib.weights` "the last element absorbs the residue so the vector sums to `WAD` **exactly**"; `split` "the last takes the remainder, so `sum(out) == amount` with no dust left behind"; plan I34; Priority: MEDIUM; Sources: RT-14)
- [x] **GL-69** — `HookStateLib`'s pack/unpack pairs are exact inverses for in-band field values, including the signed `int24` fields stored as `uint24`. (Category: VARIABLE_TRANSITION — tracks the packed `Config`/`Dynamic`/`Armed` words; Guarantee: EXPLORATORY; Priority: MEDIUM; Sources: RT-15)
- [x] **GL-70** — Each `PriceLib` conversion rounds in its documented direction and is monotone: values round down and required amounts round up, and all four conversions are non-decreasing in their input. (Category: VARIABLE_TRANSITION — tracks the four `PriceLib` conversions; Guarantee: SHOULD-HOLD — `PriceLib` header: "prices *of* AMPS round **up** → anchors never sit below the true reference … values *of* protocol assets round **down** → NAV is never overstated … counter amounts *required* round **up** → the protocol never accepts less than it is owed"; plan I20; Priority: MEDIUM; Sources: RD-10)
- [x] **GL-71** — The two independent implementations of the bond accretion floor agree exactly, and the floor is anti-monotone in both the haircut and the demanded accretion. (Category: VARIABLE_TRANSITION — tracks `qFloorX18`; Guarantee: SHOULD-HOLD — x-ray I-10: the floor is "**computed twice independently** … Rounding directions are numerator down, denominator up, quotient down in both. **If violated** — … a policy swap becomes an issuance decision"; `BondPolicy.qFloorX18` is `public pure` "so a reviewer can diff the two implementations against one call"; Priority: HIGH; Sources: RD-11)
- [x] **GL-72** — Ladder cell geometry is contiguous and the anchor residue is bounded: bucket `k`'s upper bound is bucket `k+1`'s lower bound (asks, mirrored for bids), every bound is a multiple of the tick spacing, and the first ask cell straddles the anchor by less than one spacing. (Category: VARIABLE_TRANSITION — tracks `bucketBounds` / `doublingTicks`; Guarantee: SHOULD-HOLD — `LadderLib.bucketBounds` NatSpec ("Buckets are contiguous and non-overlapping by construction (`upper_k == lower_{k+1}` above, `lower_k == upper_{k+1}` below)"); ruling AX (fix-log wave 2, `test_i32_theStraddleOfTheFirstAskCellIsBoundedByOneTickSpacing`); plan I34; Priority: MEDIUM; Sources: RD-16)
- [x] **GL-73** — `BondPolicy.discountBps` stays inside `[dMin, dMax]`, is non-decreasing in the deficit and non-increasing in the fill. (Category: VARIABLE_TRANSITION — tracks `discountBps`; Guarantee: SHOULD-HOLD — x-ray I-12 ("Bond discount `d ∈ [dMinBps, dMaxBps] ⊆ [DISCOUNT_BPS_MIN, DISCOUNT_BPS_MAX]` … The policy clamps to the same pair at `BondPolicy.sol:86-87`"); the header rounding table ("`k_c x fill` | up … it is *subtracted*, so rounding it up rounds `d` down"); Priority: MEDIUM; Sources: RD-17)
- [x] **GL-74** — Zero inputs are safe on every conversion: `previewRedeem(0)` is all zeros, `redeemProRata(0)` and `bond(m, 0, …)` revert `ZeroAmount`, `bonds.quote(m, 0).ampsOut == 0`, `pot.quote(0, 0)` pays zero, and `LadderLib.split(0, w)` is all zeros. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — explicit guards: `if (shares == 0) revert ZeroAmount();` (`AmpsVault.sol:842`), `if (amountIn == 0) revert ZeroAmount();` (`AmpsBonds.sol:367`), `if (supply == 0 || shares == 0) return …` (`VaultRedeemLib.previewUnwind:831`), `BountyPot._quote`'s chost branch; Priority: MEDIUM; Sources: RD-02)
- [x] **GL-75** — The fee the hook returns is always `base + dyn` clamped into `[F_MIN_BPS, TOTAL_FEE_BPS_MAX]`, is a whole number of basis points in pips, has `dyn` within its cap, and never reaches `MAX_LP_FEE`. (Category: VARIABLE_TRANSITION — tracks the hook's returned fee; Guarantee: SHOULD-HOLD — plan I16 ("Returned fee = `base + dyn` … `dyn ≤ dynCap_state`, total ≤ `MAX_LP_FEE`"); `Constants.sol:459-461` ("The highest total fee the hook can ever return … far below `MAX_LP_FEE`, which is what invariant I16 asserts"); Priority: MEDIUM; Sources: RD-09, SPEC-18)
- [x] **GL-76** — The credit blend never rounds a fee down in the swapper's favour: for a pass-through sell `buyFeeBps <= base <= ampsFeeBps`, the base is exactly `buyFeeBps` when the credit covers the whole input and exactly `ampsFeeBps` when it covers none, and the base is monotone non-increasing in the credit. (Category: VARIABLE_TRANSITION — tracks the hook's `baseBps`; Guarantee: SHOULD-HOLD — `AmpsHook._quote` on the blend: "Rounded up, so a credit never rounds a fee down in the swapper's favour. `mulDivRoundingUp` carries the 512-bit intermediate…"; plan I16 rev-6/7; Priority: HIGH; Sources: RD-08)
- [x] **GL-77** — The quoter and the hook never disagree about executability: a route the hook refuses pre-swap is refused by the quoter, and a quote that says executable with `degraded == 0` returns a non-zero output. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — fix-log wave-3 finding 14: "`quoteExactIn`, `quoteSellWithCredit`, `quoteRotation` and `wouldRevert` evaluate `afterSwap`'s own rule against it, reading `fairTick` and the *effective* `outerRailTicks` from the hook **so the two cannot disagree**"; Priority: MEDIUM; Sources: SPEC-20)
- [x] **GL-78** — Execution gets monotonically worse with size and never better than linear: in one pool and direction at one instant, `quoteExactIn(x)` is non-decreasing in `x` and `amountOut(2x) <= 2·amountOut(x)`. (Category: VARIABLE_TRANSITION — tracks `quoteExactIn` output against size; Guarantee: EXPLORATORY; Priority: MEDIUM; Sources: SPEC-16)
- [x] **GL-79** — `bonds.quote` is monotone non-decreasing in `amountIn` up to the capacity clamp, and the `capacityLeft` it reports does not depend on the amount quoted. (Category: VARIABLE_TRANSITION — tracks `quote.ampsOut` / `capacityLeft`; Guarantee: SHOULD-HOLD — closed form: `q` is computed from market state alone and the amount enters only as `ampsOut = mulDiv(amountIn18, q, WAD)` plus the `capacityLeft` clamp (`AmpsBonds._quote:855-897`, `BondPolicy.quote:67-92`); `mulDiv` by a fixed `q` is monotone and `min(x, cap)` preserves it; Priority: MEDIUM; Sources: RD-06)
- [x] **GL-80** — Bid inventory placed into a constituent's pool and later withdrawn by `withdrawRetiredBids` comes back at or below what went in — the vault never recovers more counter asset from a bid ladder than it placed there. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: MEDIUM; Sources: RT-09)
- [x] **GL-81** — The valuer never overstates a pool: `valuer.amountsOf(pool).counter` stays within what a full unwind could free plus the vault's claim balance, and it is zero exactly when that pool's ladder is empty. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: LOW; Sources: RD-19)

## Specific Properties
> Operation-gated postconditions. Implemented as `internal function property_*()` in `Properties.sol` and called from the relevant handler after `snapshotAfter()`.

- [x] **SP-01** — Mint attribution: across any single handler call, `totalSupply` rises by exactly the `ampsOut` that `AmpsBonds.bond` returned, and by zero in every other handler. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — x-ray X-6 (on-chain: Yes) and E-1; guards G-6 (`to != bonds_` refused) and G-40; `AmpsVault.mintVesting` is the only post-genesis mint site and `AmpsBonds.bond` its only caller; After: every handler; Priority: HIGH; Sources: CON-03, VT-01)
- [x] **SP-02** — A successful bond appends exactly one vesting position to `to` and nowhere else, and the new record is fully determined: `principal == ampsOut`, `claimed == 0`, `start == block.timestamp`, `vestSeconds == bonds.vestSeconds()` at purchase, `marketId == the market bonded`. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — the struct is written literally, once, with those five fields (`AmpsBonds.sol:411-419`) and `positionId` is the pre-push length (`:410`); After: `ampsBonds_bond*`; Priority: HIGH; Sources: ST-06, SPEC-13)
- [x] **SP-03** — A successful bond raises `totalSupply` by exactly the AMPS it issued and every wei lands in `AmpsBonds`' own ERC-20 balance — never in the bonder's, the vault's or the recipient's. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — plan I30 and x-ray X-6 ("Only `AmpsBonds` can cause AMPS to be minted post-genesis, and only into its own custody"), enforced by `if (to != bonds_) revert ZeroAddress()` around `IAmps(_AMPS).mint` (`AmpsVault.sol:978`); After: `ampsBonds_bond*`; Priority: HIGH; Sources: ST-07)
- [x] **SP-04** — Bond deposit conservation: a successful bond moves exactly `amountIn` of collateral from the bonder into the vault's holding (claims + idle), plus whatever stray balance of that collateral the shell forwarded with it, with nothing left on the bond shell. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — guard G-35 (`if (settled != amountIn) revert DepositMismatch(settled, amountIn)`, `AmpsBonds.sol:388`); plan I12 for the shell's zero balance; fix-log wave-1 finding 1 ("dust is forwarded to the vault best-effort … `CollateralForwarded`"); After: `ampsBonds_bond*`; Priority: HIGH; Sources: CON-24, CON-13)
- [x] **SP-05** — A bond is accretive and respects the floor: `ampsOut · navPerShare · (1 + minAccretionBps) <= collateralIn · P_i · (1 − hSession)` on the NAV/share the bond priced against (the value its own checkpoint stored). The "NAV/share after ≥ before" reading is implied by the identity and is not asserted preview-to-preview: every such comparison contains the reference's self-referential convergence step (lead L-1, campaigns of 2026-09-10/11), which no fixed tolerance bounds. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I27's identity; x-ray E-2 ("A bond issues at or below `navPerShare × (1 + minAccretionBps)` per unit of haircut-adjusted collateral value — issuance is accretive to NAV/share by construction"); G-38; After: `ampsBonds_bond*`; Priority: HIGH; Sources: RD-07, ADV-10, SPEC-08)
- [x] **SP-06** — No bond issues more than the capacity that was on offer immediately before it, none issues below the caller's `minAmpsOut`, capacity falls by what was issued unless the epoch rolled inside the call, and daily issuance stays under the daily cap read before the call. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — plan I28 and G-39 (`AmpsBonds.sol:401`, `SlippageExceeded`), with the clamp at `:405-410` (`ampsOut = priced.ampsOut > available ? available : priced.ampsOut`); After: `ampsBonds_bond*`; Priority: HIGH; Sources: SPEC-26, RD-05)
- [x] **SP-07** — `bonds.quote(marketId, amountIn).ampsOut` bounds the `ampsOut` the bond issues when both are read in the same block immediately after `vault.checkpoint()` — exactly, up to the ratio of the NAV/share basis the quote read to the one the bond's own checkpoint stored (`q_floor ∝ 1/navPerShare`; lead L-1) — and the quote names no refusal reason on a landed bond. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `AmpsBonds._quote` NatSpec "The pricing arithmetic is shared with `bond`; only the error handling differs" (`:829`); both recompute `ampsOut = FullMath.mulDiv(amountIn18, qX18, WAD)` from the same bounded `q` (`_price:546`, `_quote:891`); the same-block precondition is x-ray X-7; After: `ampsBonds_quoteMatchesBond` (new); Priority: HIGH; Sources: RD-05)
- [x] **SP-08** — A bond that consumed collateral issued AMPS at or above the caller's floor, and a reverted bond keeps the bonder's collateral; the "clamped surplus reaches holders as NAV" leg is SP-05's identity (the unconverted collateral lands in the vault at full value) and is not asserted preview-to-preview (lead L-1). (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; After: `ampsBonds_bond*`; Priority: MEDIUM; Sources: ADV-22)
- [x] **SP-09** — A claim moves AMPS out of `AmpsBonds` and nothing else: `totalSupply` unchanged, `principal`/`start`/`vestSeconds`/`marketId` unchanged, `claimed` up by exactly the amount transferred, the shell's balance down by exactly that amount, and the position shows no pending amount afterwards (`claimAll` leaves `claimableTotal == 0`). (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — `record.claimed = uint128(vested)` immediately before `transfer(to, amount)` with `amount = vested - record.claimed` (`AmpsBonds.sol:563-569`; `claimAll` at `:576-591`); `Amps.mint/burn` are `onlyVault` (`Amps.sol:34`); After: `ampsBonds_claim*`, `ampsBonds_claimAll*`; Priority: HIGH; Sources: ST-08, SPEC-24)
- [x] **SP-10** — A redemption burns exactly the shares it was given plus the inventory it released: `totalSupply` falls by `shares + inventoryBurned` (the figure `previewRedeem` predicted a moment earlier) and the caller's balance falls by exactly `shares`. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — plan I23 ("`redeemProRata` … burns the released inventory AMPS"); `IAmps.burn(msg.sender, shares)` (`AmpsVault.sol:848`) and `IAmps.burn(address(this), result.inventoryBurned)` (`:882`) are the only two supply writes, with `supply` read at `:845` **before** the burn (x-ray I-9); After: `ampsVault_redeemProRata*`; Priority: HIGH; Sources: CON-04, ST-10, RD-01)
- [x] **SP-11** — The redemption preview is the redemption payout: for every token in the basket the recipient's combined gain (ERC-20 balance plus ERC-6909 claim, since the payout may fall back to claims) equals what `previewRedeem(shares)` predicted, and never more than the vault held. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `AmpsVault.previewRedeem` NatSpec "the preview and the payout agree to the wei" (`:555-557`); `VaultRedeemLib.previewUnwind` "Mirrors v4's own decomposition exactly … so `previewRedeem` and `redeemProRata` agree to the wei" (`:808-811`); fix-log wave 4 restates it; After: `ampsVault_redeemProRata*`; Priority: HIGH; Sources: RD-01, CON-25, SPEC-07)
- [x] **SP-12** — One user cannot operate on another's position: a redemption burns only the caller's own shares (no allowance path exists) and a claim touches only the caller's own vesting positions — no third party's AMPS balance or claimable total moves. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — `IAmps(_AMPS).burn(msg.sender, shares)` with the NatSpec "the redeemer's shares are gone before a single asset moves"; `AmpsBonds.claim` "Structurally ungated. The only reads are `_positions[msg.sender]` and the immutable {amps}"; plan I38; After: `ampsVault_redeemProRata*`, `ampsBonds_claim*`, `adversary_crossUserRedeem` (new); Priority: HIGH; Sources: ST-10, ADV-26)
- [x] **SP-13** — A non-dust redemption is accretive to the holders who stay: for `shares >= 1e12` with a non-zero fee, NAV/share after is at least NAV/share before; with a zero fee it falls by at most 2 bp (the `VIRTUAL_SHARES` dust). (Category: VARIABLE_TRANSITION — tracks `navPerShareX18` across a redemption; Guarantee: SHOULD-HOLD — fix-log wave 4 on `keepBps` and the inventory burn: "That is the protocol-favourable direction (inventory AMPS is worth zero in `A` by I5, so the larger burn lowers `T` and raises NAV/share for the remaining holders)"; plan I5 and I23; After: `ampsVault_redeemProRata*`; Priority: HIGH; Sources: SPEC-09, ADV-09)
- [x] **SP-14** — A single redemption never pays out more USD value than its pro-rata slice of `A` net of the redemption fee: `value(basket) <= A · shares / T · (BPS − redeemFeeBps) / BPS + dust`. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; After: `ampsVault_redeemProRata*`; Priority: HIGH; Sources: ADV-09)
- [x] **SP-15** — A redemption never creates, destroys or re-shapes a ladder record: for every pool `ladderLength` is unchanged and every record's `lowerTick`, `upperTick` and `above` are unchanged; only `liquidity` may fall. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — plan I35 and the `PlacementRecord` NatSpec "Ladders are static: a record is written when the bucket is placed and is only ever *removed* by pro-rata redemption, rollout …, the high-water buyback burn, or migration. It is never re-centred, re-widened or moved up." (`Types.sol:426-429`); `unwind` writes only `record.liquidity = live - removed` (`VaultRedeemLib.sol:757`); After: `ampsVault_redeemProRata*`; Priority: HIGH; Sources: ST-11)
- [x] **SP-16** — A full-balance redemption never reverts: whatever gate state, guardian freeze, divergence breaker, retired constituent, donated dust or hostile balance the campaign has produced, `redeemProRata(balance, actor)` succeeds. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I14; the protocol summary's "unpausable" redemption; fix-log wave-1 findings 2/3 and wave-4 finding 8 (claims-only fallback) exist to keep this true; After: `ampsVault_redeemProRata_full`; Priority: HIGH; Sources: ADV-01)
- [x] **SP-17** — R1: every placement path leaves `navPerShareAfter >= navPerShareBefore · (1 − 2 bp)`, with both sides read on the same basis. (Category: VARIABLE_TRANSITION — tracks `navPerShareX18` across a placement; Guarantee: SHOULD-HOLD — plan I11 verbatim and guard G-20 `if (navAfter < floor) revert NavBleedExceeded(...)` (`AmpsVault.sol:1459`) with `PLACEMENT_BLEED_BPS_MAX = 2` (`Constants.sol:643`); x-ray E-3; After: `_ampsVault_place`, `ampsVault_compound`, `ampsVault_rollout`, `ampsVault_deployBonded`, `_ampsVault_withdrawRetiredBids`; Priority: HIGH; Sources: ST-12, ADV-12, SPEC-08)
- [x] **SP-18** — A placement conserves inventory and lays the shape the policy asked for: the vault's AMPS (idle + claim) falls by exactly what was placed on the ask side and is unchanged on the bid side, nothing is minted to fill an ask, and bucket `k` carries `tilt^k / Σ tilt^j` of the placement to within `buckets` wei. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I10 ("the vault's ask inventory … **is never minted**") and I34; `VaultPlacementLib._split:1260-1273` and `compound:373-400` cancel exactly; `LadderLib.split` is documented exact; After: `_ampsVault_place`, `ampsVault_compound`; Priority: HIGH; Sources: CON-23, SPEC-19)
- [x] **SP-19** — Sidedness holds unconditionally at placement: every record this call wrote has an ask at or above the current sqrt price and a bid at or below it, and no freshly placed cell holds both sides. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — plan I9 and G-30 (`VaultPlacementLib.sol:873`, `WrongSide`: "an ask holds only AMPS, a bid only the counter, so no cell is straddled at placement"); fix-log wave-4 finding 2 (`_executePlace` skips a bucket whose cell already holds the other side); After: every placement-class handler; Priority: HIGH; Sources: SPEC-14)
- [x] **SP-20** — A placement cannot be sandwiched into a manipulated tick: every successful `place`, `compound`, `rollout` and `deployBonded` was within `PLACEMENT_DIVERGENCE_TICKS` of fair at both entry and exit. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — G-26 (`VaultPlacementLib.sol:574`) — "Checked at entry and exit so a placement cannot be sandwiched into a manipulated tick"; After: every placement-class handler; Priority: HIGH; Sources: ADV-30)
- [x] **SP-21** — A call that placed nothing and bought nothing back takes no cooldown, arms no surge and resets no high-water mark; and the mark is never reset without a buyback. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — fix-log wave-3 finding 1 ("the mark on `boughtBack != 0`, the surge on `placed != 0`, the cooldown on `placed != 0 || burned != 0`") and wave-4 finding 5 (`COMPOUND_PLACE_MIN_USD18`); `VaultPlacementLib.sol:288` and `:464-466`; x-ray I-18; After: `_ampsVault_place`, `ampsVault_compound`, `ampsVault_rollout`, `ampsVault_deployBonded`; Priority: HIGH; Sources: ST-17, ADV-04)
- [x] **SP-22** — A record's `above` flag changes only while the cell is empty: if the cell held liquidity before the call, its side is unchanged after it. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — `_writeRecords`: "`above` is written only when the cell is opened (audit fix, 2026-09-09) … `bool opensCell = record.liquidity == 0; if (opensCell) { ++opened; record.above = p.above; }`" (`VaultPlacementLib.sol:930-943`); After: every placement-class handler; Priority: MEDIUM; Sources: ST-15)
- [x] **SP-23** — The buyback burns only what it bought back: `_burnback` burns an `above` cell whose upper bound the high-water mark has crossed and whose lower bound is at or above the current tick — never a bid cell and never an ask under a stale mark — and cumulative burn never exceeds cumulative buyback. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — plan I33; fix-log wave-1 finding 4 ("burn only `upperTick <= highWater && tick <= lowerTick`"), wave-3 finding 15 ("no bid — however far the price has fallen through it — is burned as a buyback"), wave-4 finding 2; After: `ampsVault_compound`, `_ampsVault_place`; Priority: HIGH; Sources: ADV-31)
- [x] **SP-24** — `compound` never increases supply and reduces it by exactly the `burned` it reports — the AMPS-side fee remainder plus the whole high-water buyback, with no AMPS retained as inventory. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — revision 6/7 amendment to I33 ("buybacks and AMPS-side fees burned after the creator slice"); `VaultPlacementLib._split` computes `burnCut = ampsFees - creatorPaid` followed by `IAmps(amps).burn`, and `compound` returns `burned = boughtBack + feeBurn + burnCut`; x-ray E-1 lists `VaultPlacementLib.sol:257/383/1271` as burn-only sites; After: `ampsVault_compound`; Priority: HIGH; Sources: CON-05, ST-16)
- [x] **SP-25** — The creator's slice is bounded by volume in both currencies: the AMPS gain is at most `ampsFees × CREATOR_FEE_BPS / AMPS_FEE_BPS_DEFAULT`, the counter-side claim gain at most the same fraction of `counterFees`, and the gain is exactly zero from `genesisTimestamp + CREATOR_DECAY_SECONDS` onward. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I31 as amended by revision 6/7; x-ray I-21 (temporal, on-chain: Yes); the divisor is floored at `AMPS_FEE_BPS_DEFAULT` and raised to `chargedFeeBps`, both pushing the payout down (fix-log wave-3 #4, wave-4 #3); After: `ampsVault_compound`; Priority: MEDIUM; Sources: CON-27)
- [x] **SP-26** — Any call that reaches `_checkpoint()` leaves `checkpointData().timestamp == block.timestamp` and `.blockNumber == uint32(block.number)`; `touch` deliberately leaves both where it found them. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — `_checkpointTimestamp`/`_checkpointBlock` are written inside `_checkpoint`, which `_afterPlacement` calls unconditionally (`AmpsVault.sol:1457`, `:1533-1534`); `touch`'s NatSpec "It deliberately does not touch `checkpointTimestamp`" (`:915-916`); After: `ampsVault_checkpoint`, `ampsVault_touch`, `ampsBonds_bond*`, every placement-class handler; Priority: MEDIUM; Sources: ST-13)
- [x] **SP-27** — `sweepClean` holds: after any `AmpsVault` entry point the vault's idle ERC-20 balance of every registered asset is zero — everything the protocol owns is a claim or a position, never a loose balance. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I12 ("stock ERC-20 balance of vault, hook and `AmpsBonds` == 0 after every external function"); `AmpsVault._sweepClean()` is called from every entry point (`AmpsVault.sol:887, 909, 921, 969, 983, 1006, 1051, 1075`, `_afterPlacement:1460`); After: every `AmpsVaultHandler` / `AmpsBondsHandler` entry point that reaches the vault; Priority: MEDIUM; Sources: CON-11)
- [-] **SP-28** — The rollout window is charged on the realised drain: the increase in the decayed `rolloutMoved24h` accumulator equals the `moved` the call returned, and `windowStart` is re-stamped whenever anything was charged. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; After: `ampsVault_rollout`; Priority: LOW; Sources: CON-31)
- [x] **SP-29** — The bounty pot charges its rolling window for exactly what it transferred and never overpays: the balance drop equals what the keeper received, is within `budgetLeftRaw()` and within the balance, the window charge equals the transfer in USD18, and a zero payment charges nothing. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `BountyPot.pay` NatSpec "Effects before interactions: the window is charged for exactly what is transferred, before the transfer. A zero payment charges nothing and still emits"; the body computes `paidUsd18 = paidRaw * _usdScale` then `_chargeWindow` before `safeTransfer` (`:363-370`, `:377-386`); plan I21; After: `ampsVault_compound`, `ampsVault_rollout`, `ampsVault_deployBonded`; Priority: MEDIUM; Sources: RD-12, CON-18, ST-22)
- [x] **SP-30** — The bounty pot is segregated from backing: funding it, and paying out of it, changes `A` by zero. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I21 ("`BountyPot` excluded from `A`"); `BountyPot` NatSpec "The pot's balance is deliberately excluded from the NAV numerator `A` … The vault never reads {balance} when it checkpoints"; After: `bountyPot_secondary`, every keeper handler; Priority: MEDIUM; Sources: CON-19)
- [x] **SP-31** — An empty bounty pot never reverts a keeper path: `compound`, `rollout` and `deployBonded` succeed unpaid when the pot is dry or its budget is spent. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I21 ("depleted ⇒ unpaid, not reverting"); `BountyPot._quote` NatSpec "Pure arithmetic plus one `balanceOf`, so it cannot revert for any input a keeper or the vault can produce … every cap is a `min`"; `pay`'s `if (paidRaw != 0) { … }`; After: `ampsVault_compound`, `ampsVault_rollout`, `ampsVault_deployBonded`; Priority: HIGH; Sources: ADV-05)
- [x] **SP-32** — A swap moves the counter token, it does not create or destroy it: across `buy`/`sell`/`rotate` the payer's decrease equals the PoolManager's increase plus the recipient's and the router's, for each leg's counter asset. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — closed-form ERC-20 identity: the counter assets have no mint path reachable from a swap and every v4 swap settles as a transfer into or a `take` out of the PoolManager; plan I13 ("no `donate()` or `poolManager.swap()` anywhere in the codebase"); After: `ampsRouter_buy*`, `ampsRouter_sell*`, `ampsRouter_rotate*`; Priority: MEDIUM; Sources: CON-22)
- [x] **SP-33** — Trade attribution is exact on both sides: the recipient gains exactly the amount the router reported (plus, when the recipient is the caller, whatever dust of that asset the router was holding and swept to its caller — the GL-39 sweep, counted separately; 2026-09-12 campaign), and the payer's input balance never falls by more than `amountIn` (the unfilled remainder is swept back). (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `AmpsRouter._buyAction` NatSpec "**The realised input, not the requested one** (audit fix, 2026-09-08) … The unspent remainder stays here and is swept back to the caller by the entry point" (`:302-310`); `buy`/`sell`/`rotate` return the value decoded from the unlock result (`:186-192`, `:213-221`, `:249-257`); After: `ampsRouter_buy*`, `ampsRouter_sell*`, `ampsRouter_rotate*`; Priority: MEDIUM; Sources: RD-15, CON-21)
- [x] **SP-34** — A swap never takes more out of a pool than the pool held: a buy's `ampsOut` is at most the AMPS the ask ladder held before it, and a sell's output at most the counter the bid cells held. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; After: `ampsRouter_buy*`, `ampsRouter_sell*`; Priority: MEDIUM; Sources: SPEC-15)
- [x] **SP-35** — A swap never changes the share count: `amps.totalSupply()` is identical before and after every trade, and the hook holds neither AMPS nor an AMPS claim afterwards. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — x-ray E-1 (supply moves only at genesis, bond, redemption, the compound fee burn and the high-water buyback — a swap is none of them); plan I13; After: `ampsRouter_buy*`, `ampsRouter_sell*`, `ampsRouter_rotate*`; Priority: HIGH; Sources: SPEC-10)
- [x] **SP-36** — Honest trading never reverts for a non-rail reason: a swap the quoter says is not refused must not revert, a revert must carry a rail/slippage/deadline reason, and a deviation-reducing swap is never refused. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I15 ("No swap reverts for a gate reason or inside the outer rail; only deviation-increasing swaps beyond the rail revert"); G-45 ("The one deliberate swap refusal in the protocol"); fix-log wave-4 lead on the `amountSpecified == type(int256).min` panic and finding 1 (a pool must not be pinned at its rail); After: `ampsRouter_buy*`, `ampsRouter_sell*`, `ampsRouter_rotate*`; Priority: HIGH; Sources: ADV-03)
- [x] **SP-37** — `AmpsQuoter.quoteExactIn` predicts what the router actually gets: when the quote is neither refusing nor degraded the realised output equals it to the wei, it never overstates a degraded quote, and a swap never reverts on a quote that said it would execute. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `QuoterSwapLib` NatSpec, **Exactness**: "The step arithmetic, the ordering of the tick shift and the `zeroForOne ? tickNext - 1` convention reproduce what the PoolManager does to `slot0`, which is what makes a quote comparable to a real swap to the wei"; After: `ampsRouter_buy*`; Priority: HIGH; Sources: RD-03)
- [x] **SP-38** — `AmpsQuoter.quoteRotation` predicts `AmpsRouter.rotate` to the wei, including the credit it says hop 2 will consume. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `AmpsRouter._rotateAction` "**Hop 2 sells the realised delta of hop 1, never a quoted number** … so hop 2's credit covers hop 2's input exactly and the blend resolves to `buyFeeBps`"; `AmpsQuoter.quoteRotation` NatSpec on the same blend, plus `QuoterSwapLib`'s wei-exactness claim; After: `ampsRouter_rotate*`; Priority: HIGH; Sources: RD-04)
- [x] **SP-39** — The rotation fee schedule is exactly the revision-6 law: hop 1's base is that pool's `buyFeeBps`, hop 2's base sits between `buyFeeBps(hop2)` and `ampsFeeBps`, `dyn` is within its cap and the total within `TOTAL_FEE_BPS_MAX`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — plan I16 with the revision-6/7 amendment ("base ∈ {buyFee on a ROTATE hop from the router, ampsFeeBps, the credit blend}"); fix-log wave-3 finding 3; After: `ampsRouter_rotate*`; Priority: HIGH; Sources: SPEC-18, RD-08)
- [x] **SP-40** — The rotation exemption cannot be bought for a route that is not a rotation: `rotate` refuses a same-pool route (`SameHop`) and a route with no constituent spoke leg (`NotARotation`), and an ordinary buy or sell is never charged the pass-through base. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — fix-log wave-3 finding 3 ("**Fixed, both halves** — `rotate` requires at least one constituent leg (`NotARotation`)", tests `RotationCreditGaming.t.sol::test_f03_anEntryToEntryRotationIsRefused`); plan I26 rev-6/7 ("credit only for the router's ROTATE hops"); `AmpsHook._isPassThrough` (`sender == router && flag == ROUTER_ROTATE`); After: `ampsRouter_rotate*`; Priority: HIGH; Sources: ADV-15)
- [x] **SP-41** — An atomic two-call wash pays the AMPS fee on the second pass: two `rotate` calls over the same pair inside one transaction cannot both be priced at the pass-through base, and no rotation credit survives the transaction. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — fix-log wave-3 finding 3, **Fixed**: "`AmpsHook` counts the pass-through hops it has priced per pool per transaction in transient storage, so the second pass of a two-call round trip pays `ampsFeeBps` while an honest one-hop-per-pool rotation is untouched" (regression `RotationCreditGaming.t.sol::test_f03_aTwoCallRoundTripPaysTheAmpsFeeOnTheSecondPass`); After: `ampsRouter_rotate_twiceInOneTx` (new); Priority: HIGH; Sources: RT-04, ADV-16)
- [x] **SP-42** — Buying AMPS out of a pool and immediately selling it back into the same pool leaves the actor with no more of the counter asset and no more AMPS than they started with; a refused second leg is not a round trip and is skipped. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; After: `ampsRouter_buySellRoundTrip` (new); Priority: HIGH; Sources: RT-01)
- [x] **SP-43** — Selling AMPS into a pool and immediately buying back with the whole proceeds leaves the actor with no more AMPS and no more counter asset than they started with; a refused buy-back (a pool with no asks left) is not a round trip and is skipped (2026-09-11 campaign). (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; After: `ampsRouter_sellBuyRoundTrip` (new); Priority: HIGH; Sources: RT-02)
- [x] **SP-44** — Rotating A -> AMPS -> B and then rotating the whole proceeds back never returns more of the entry asset than went in. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; After: `ampsRouter_rotateRoundTrip` (new); Priority: HIGH; Sources: RT-03)
- [x] **SP-45** — Buying AMPS out of a pool and immediately redeeming it pro-rata does not return more of the pool's counter asset than the buy consumed, and leaves no extra AMPS; asserted only when `previewRedeem`'s counter amount does not exceed what the buy paid, since a pool quoting below the redemption's counter content per share is the floor arbitrage, not rounding (2026-09-12 campaign). (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; After: `ampsVault_buyRedeemRoundTrip` (new); Priority: HIGH; Sources: RT-07)
- [x] **SP-46** — [C2] Repeated cycles extract nothing: running `buy -> redeemProRata` N times in a single call, each cycle feeding the previous cycle's output, never leaves the actor with more of the counter asset than the first cycle's input — dust does not accumulate into a drain; a cycle the floor arbitrage would pay (preview above payment) disarms the assertion (2026-09-12 campaign). (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; After: `ampsVault_buyRedeemCycles` (new, dedicated round-trip handler); Priority: HIGH; Sources: C2, RT-07, RT-16)
- [x] **SP-47** — A bond followed by a claim after the vest and a redemption of exactly the AMPS the bond minted returns no more of the bonded collateral than was deposited, and leaves NAV/share no lower than before the bond. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — x-ray E-2 ("issuance is accretive to NAV/share by construction") and plan I27 ("NAV/share after any bond ≥ NAV/share before"); redemption raises NAV/share too (fix-log wave 4: "the larger burn lowers `T` and raises NAV/share for the remaining holders"); the collateral leg is the EXPLORATORY half of the same handler; After: `ampsBonds_bondClaimRedeemRoundTrip` (new); Priority: HIGH; Sources: RT-08)
- [-] **SP-48** — The book fills from the inside out: a buy consumes AMPS from the ask cell nearest the tick first — no cell at index `m` loses AMPS while a cell at `m' < m` still holds any — and symmetrically for a sell against the bids. (Category: STATE_TRANSITION; Guarantee: EXPLORATORY; After: `ampsRouter_buy*`, `ampsRouter_sell*`; Priority: MEDIUM; Sources: SPEC-29)
- [x] **SP-49** — No free AMPS from sub-unit truncation: any router buy that produced `ampsOut > 0` consumed a non-zero realised input and paid a non-zero fee, and the counter value in covers the AMPS out at a sane fraction of NAV/share. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; After: `ampsRouter_buy*` (notably `ampsRouter_buy_dust`); Priority: HIGH; Sources: ADV-21)
- [x] **SP-50** — A constituent's status moves only along the two legal edges: ACTIVE -> {ACTIVE, RETIRED}, RETIRED -> {RETIRED, ACTIVE}, and NONE never becomes anything. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — explicit enum guards `if (config.status != ConstituentStatus.ACTIVE) revert InvalidStatusTransition(...)` (`PoolRegistry.sol:340`) and `if (config.status != ConstituentStatus.RETIRED) revert InvalidStatusTransition(...)` (`:362`); `Types.sol` documents NONE as "never registered"; After: `poolRegistry_secondary`; Priority: HIGH; Sources: ST-01)
- [x] **SP-51** — `activeConstituentCount()` changes by exactly +1 on a RETIRED -> ACTIVE edge, −1 on an ACTIVE -> RETIRED edge, and by zero otherwise. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — Δ-pair: `config.status = RETIRED` (`PoolRegistry.sol:344`) with `_activeCount = _activeCount - 1` (`:347`) in one `unchecked` block, and `config.status = ACTIVE` (`:370`) with `_activeCount = _activeCount + 1` (`:374`); After: `poolRegistry_secondary`; Priority: HIGH; Sources: ST-02)
- [x] **SP-52** — Retiring a name zeroes its rollout weight and stamps `retiredAt`; reinstating clears `retiredAt` and installs exactly the rollout weight the call asked for. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — plan I37, enforced at `PoolRegistry.sol:345-346` (`rolloutWeightBps = 0; retiredAt = block.timestamp`) and `:369-372` (`rolloutWeightBps = arg; retiredAt = 0`); After: `poolRegistry_secondary`; Priority: MEDIUM; Sources: ST-03)
- [x] **SP-53** — Retiring or reinstating one constituent leaves every other constituent's record byte-identical (`setIndexWeights` exempt, since it rewrites the whole active set). (Category: STATE_TRANSITION; Guarantee: EXPLORATORY; After: `poolRegistry_secondary`; Priority: MEDIUM; Sources: ST-05)
- [x] **SP-54** — `divergedSince[pool]` moves only 0 -> now (latch) or t -> 0 (clear); it never moves from one non-zero value to a different non-zero value. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — the only two writes are guarded on the current value: `if (outside && since == 0) _divergedSince[poolId] = uint32(block.timestamp); else if (ok && !outside && since != 0) delete _divergedSince[poolId];` (`OracleGate.sol:1388-1395`); After: `oracleGate_secondary` (poke variants); Priority: MEDIUM; Sources: ST-18)
- [x] **SP-55** — A token's accepted answer changes only through a latch, and a latch always advances the round: if the accepted answer or its stamp changed, the round id changed too (or the previous answer was zero). (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — `_latch` is the only writer of `_accepted[token]` (`FeedRegistry.sol:748`), and `refresh` reaches it only under `else if (roundId != accepted.roundId || accepted.answerUsd8 == 0)` (`:377`); After: `feedRegistry_secondary`; Priority: MEDIUM; Sources: ST-20)
- [x] **SP-56** — A latch clears the pending record: if the accepted round id changed in this call, `pendingAnswer(token).roundId == 0` afterwards. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — `_latch` writes the accepted answer and then `if (_pending[token].roundId != 0) delete _pending[token];` (`FeedRegistry.sol:747-751`); After: `feedRegistry_secondary`; Priority: MEDIUM; Sources: ST-21)
- [x] **SP-57** — A feed refresh and a bond in the same transaction cannot be front-run into a favourable price: the bond prices the held-back answer (`min(held, candidate)`), never the candidate, and refuses outright while the vault's NAV is built on an unconfirmed answer. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — fix-log wave-1 finding 9, **Fixed** ("registry reports `min(held, candidate)` while held; gate treats `unconfirmed` as stale and floors the haircut at `CLOSED`"), and wave-2 finding 7 ("`AmpsBonds._price` refuses (`UnconfirmedNav`)", `AmpsBonds.fuzz.t.sol::testFuzz_anUnconfirmedNavRefusesEveryBond`); x-ray G-36, G-37, X-4, X-7; After: `adversary_refreshThenBond` (new); Priority: HIGH; Sources: ADV-29)
- [x] **SP-58** — A governance call is value-neutral in the instant it lands: no setter moves an asset, changes `amps.totalSupply()`, changes any actor's AMPS balance, changes the vault's holding of any registered asset, or moves NAV/share. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; After: `ampsVault_secondary` (setter selectors), `ampsBonds_secondary`, `ampsHook_secondary`, `poolRegistry_secondary`, `oracleGate_secondary`; Priority: MEDIUM; Sources: SPEC-32, SPEC-08)
- [x] **SP-59** — A donation bricks nothing and is never recovered by the donor: after any ERC-20 donation (AMPS included) to the vault, hook or bond shell and after any forced ether transfer, every entry point that succeeded before still succeeds, and the donor's value does not rise. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — fix-log wave-1 finding 1 (`AmpsBonds._issue` donation brick, **Fixed**, `test_collateralDonationCannotBrickTheMarket`), wave-1 finding 3 (`test_aDonatedWeiOfAFrozenTokenDoesNotBrickTheOtherEntryPoints`), wave-3 finding 12; After: `ampsVault_donateERC20`, `ampsVault_donateETH`; Priority: HIGH; Sources: ADV-07)
- [x] **SP-60** — Full-amount operations leave the protocol re-enterable: after a full-balance redemption, a whole-inventory placement, a whole-capacity bond or a whole-balance sell, a subsequent bond and a subsequent buy both succeed and the placement bleed bound still holds. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; After: `ampsVault_redeemProRata_full`, `ampsRouter_sell_full`, `ampsBonds_bond_full`, `ampsVault_place_full` (new), `ampsBonds_bond_wholeCapacity` (new); Priority: HIGH; Sources: ADV-24)
- [x] **SP-61** — The two neutral ERC-20 operations are neutral: a self-transfer of any amount within balance leaves the sender's balance and `totalSupply` unchanged and does not revert, and a zero-value transfer to a non-zero address succeeds and moves nothing. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — ERC-20 MUST-clause "Transfers of 0 values MUST be treated as normal transfers and fire the `Transfer` event", plus OZ's `_update` identity for `from == to`; `Amps` claims plain ERC-20 with "no transfer hooks, callbacks, fees, pausing, blocklists, rebases" (`token/Amps.sol:9-13`); After: `amps_secondary`; Priority: MEDIUM; Sources: SPEC-02)
- [x] **SP-62** — Transfer and allowance accounting is exact: `transfer` moves exactly the amount and leaves `totalSupply` alone; `transferFrom` additionally decrements the allowance by exactly the amount, except an infinite allowance, which is not decremented. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — ERC-20 `transfer`/`transferFrom`/`approve` clauses; the infinite-allowance carve-out is OZ's documented `_spendAllowance` behaviour, inherited unmodified; After: `amps_secondary`, `ampsRouter_sell*` (which pull with `safeTransferFrom`); Priority: MEDIUM; Sources: SPEC-03)
- [x] **SP-63** — Every `to` parameter credits `to` and never `msg.sender` when the two differ: a bond pushes its position onto `to`'s array only, a claim pays `to`, and a redemption pays the basket to `to` while burning `msg.sender`'s shares. (Category: STATE_TRANSITION; Guarantee: EXPLORATORY; After: `ampsBonds_bond`, `ampsBonds_claim`, `ampsBonds_claimAll`, `ampsVault_redeemProRata`; Priority: MEDIUM; Sources: SPEC-13)
