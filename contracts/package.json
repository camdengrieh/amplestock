{
  "name": "@amplestocks/contracts",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "description": "Amplestocks Foundry workspace (token, vault, hook, bonds, router, policy, oracle, registry)",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "fmt": "forge fmt --check",
    "lint": "forge lint",
    "broadcast-test": "bash test/script/broadcast.sh",
    "preflight": "forge script script/00_Preflight.s.sol"
  }
}
