# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
.next/
out/
cache/
generated/
*.tsbuildinfo

# foundry (contracts/ has its own .gitignore too; these cover the
# per-agent scratch profiles used to avoid sharing out/cache dirs)
contracts/out-*/
contracts/cache-*/
contracts/broadcast/
contracts/.forge-snapshots/
contracts/lcov.info

# turbo
.turbo/

# ponder runtime state (PGlite, logs, generated views); `ponder-env.d.ts` IS committed
.ponder/

# env
.env
.env.*
!.env.example

# coverage
coverage/
lcov.info
*.lcov

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# editors / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.log

# wagmi CLI output: `packages/abis/src/generated.ts` is DELIBERATELY COMMITTED (Phase 4). The indexer, the
# dApp and the keeper consume it as ordinary TypeScript, so a checkout with no Foundry toolchain still
# typechecks, builds and tests. Regenerate with `pnpm --filter @amplestocks/abis generate`.

# licence gate report (CI artifact)
licence-report.json
